# Sesame-TK v0.5.9 更新日志

### 🌐 网络抓包与调试优化
* **RPC 最佳实践精准去重**：
  * 重构 H5/小程序与底层 RPC 的上下层去重机制，由基于时间窗口去重全面升级为**基于「接口 Method + 核心业务入参签名」**精准比对；
  * 提取并规范化上层 Bridge 的 `requestData` 与底层 `RpcInvocationHandler` 的 `rpcArgs[1]` 核心入参，即使底层响应先于 H5 回调触发，也能精准拦截底层镜像并输出完整 `[H5]` 结构化日志；
  * 原生 Native 与小游戏 RPC 无缝保留 `[BOTTOM]` 捕获，彻底杜绝日志重复与误吞现象。
* **灵活管理系统噪音过滤**：
  * 将原代码硬编码的内置黑名单解耦为预设噪音列表，用户可在抓包界面自由增删预设噪音或一键恢复默认；
  * 移除宽泛的全局 `alipay.client` 过滤，避免误杀正常 H5/小程序业务 RPC；
  * 修复黑名单管理底部弹窗向上展开时的画面抖动问题。

### 🎁 支付宝会员商品与秒杀
* **全量与精准拉取分类商品**：
  * 修复"全部商品"分区接口 `withPointRange` 参数，解决选择万分好物（10001+ 积分）却被服务端返回 10 积分低价商品的问题；
  * 修复"日常兑换"分类请求参数（`filterExchangeTime: false`, `blackIds: []`），引入多页自动翻页循环，一次性完整同步日常抢兑全部 70+ 款商品；
  * 优化会员积分查询，增加超时降级兜底，避免阻塞商品同步。
* **同步超时保护机制**：
  * 在商品秒杀页面引入 15 秒超时守护计时器，覆盖顶栏刷新、空白重试与服务端搜索等全部触发路径，杜绝请求卡死。
* **存储与目录识别修复**：
  * 优化用户配置目录识别逻辑（`Files.isUserDirectory`），主动排除 `member_goods`、`cache`、`logs` 等系统数据目录，彻底解决会员商品文件夹被误识别为多账号配置的问题。

### ⚡ 稳定性与体验优化
* **深色模式加载优化**：
  * 修复 `WebSettingsActivity` 启动时 `applyDarkMode is not defined` 控制台报错，增加安全前置判定。
* **炼金工坊体力管理**：
  * 优化炼金工坊体力药水使用逻辑，保持体力爆棚以提升产出效率。
